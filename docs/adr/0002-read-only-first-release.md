# Make the first release read-only

The first AdaptUI release will permit only verified non-mutating MCP capabilities and will reject or hide mutating capabilities. When a capability cannot be classified confidently, AdaptUI will fail closed; action approval workflows can be added later once the MCP contract and user experience are understood.
